import { PermissionGate } from "@/components/shared/PermissionGate";
import { WorkspacePage } from "@/components/workspace/WorkspacePage";
export default function Page({ params }: { params: { id: string } }) { return <WorkspacePage eyebrow="Sales / CRM" title={`Lead ${params.id}`} description="CRM detail with customer context, requirements and the complete commercial activity trail."><section className="grid gap-4 lg:grid-cols-3"><article className="space-y-5 rounded-[24px] border border-line bg-surface p-6 lg:col-span-2">{["Lead Information", "Requirements", "Activity Timeline", "Notes", "Documents", "Quote", "Tasks"].map((item) => <div key={item} className="border-b border-line-faint pb-5 last:border-0"><h2 className="text-sm font-semibold">{item}</h2><p className="mt-2 text-xs text-ink-dim">This section binds to the lead detail API contract when available.</p></div>)}</article><aside className="rounded-[24px] border border-line bg-surface p-6"><h2 className="text-sm font-semibold">Actions</h2><div className="mt-5 grid gap-3"><PermissionGate permission="lead:assign"><button className="rounded-full bg-accent px-4 py-3 text-xs font-bold uppercase text-accent-fg">Assign</button></PermissionGate>{["Change Stage", "Add Note", "Create Task", "Send Quote", "Schedule Call"].map((action) => <button key={action} className="rounded-full border border-line px-4 py-3 text-xs font-bold uppercase text-ink-dim">{action}</button>)}</div></aside></section></WorkspacePage>; }
