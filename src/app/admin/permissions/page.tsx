import { ROLE_PERMISSIONS } from "@/config/access";
import { WorkspacePage } from "@/components/workspace/WorkspacePage";
export default function Page() { const roles = Object.entries(ROLE_PERMISSIONS); return <WorkspacePage eyebrow="Admin / Access" title="Permission Matrix" description="UI rendering is permission-driven. Backend enforcement remains mandatory."><div className="overflow-x-auto rounded-[24px] border border-line bg-surface"><table className="w-full text-left text-xs"><thead><tr className="border-b border-line"><th className="p-4">Role</th><th className="p-4">Permissions</th></tr></thead><tbody>{roles.map(([role, permissions]) => <tr className="border-b border-line-faint" key={role}><td className="p-4 font-bold text-accent">{role}</td><td className="p-4 text-ink-dim">{permissions.join(" · ")}</td></tr>)}</tbody></table></div></WorkspacePage>; }
