import { WorkspacePage } from "@/components/workspace/WorkspacePage"; import { GapNotice } from "@/components/workspace/GapNotice";
export default function Page() { return <WorkspacePage eyebrow="Admin / Access" title="Permission Administration" description="Dynamic permission administration is not part of the current backend contract."><GapNotice tone="blocked" title="Permissions are read-only policy, not an admin CRUD surface." source="Backend OpenAPI / Admin authority"><p>The frontend does not expose editable role, permission or membership controls.</p></GapNotice></WorkspacePage>; }
