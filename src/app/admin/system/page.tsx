import { SimpleView } from "@/components/workspace/DashboardView"; export default function Page() { return <SimpleView eyebrow="Admin / Monitor" title="System Monitor" description="Monitoring UI and data contract; status must be supplied by backend health services." sections={["API — ONLINE", "Database — ONLINE", "Authentication — ONLINE", "Storage — ONLINE", "Payment Service — DEGRADED", "KYC Service — ONLINE", "GPU Provisioning — ONLINE"]} />; }
